@model List<BookCave.Models.ViewModels.CartViewModel>

<h1>Karfa</h1>
<div class="container">
    <div class="row"></div>
        <div class="col-lg-12 cl-md-6 col-sm-6 col-xs-6">
            <table class="table table-striped table-hover">
                <tr>
                    <th>Mynd</th>
                    <th>Titill</th>
                    <th>Magn</th>
                    <th>Afsláttur</th>
                    <th>Verð</th>
                </tr>
                @foreach(var book in @Model)
                {
                    <tr>
                        <td><img src="@book.Image"  height="200" width="120" /></td>
                        <td>@book.Title</td>
                        <td>Magn: @book.Quantity</td>
                        <td>@book.Discount %</td>
                        <td>@book.Price $</td>
                    </tr>
                }
            </table>
            <button class="btn btn-primary"><a asp-controller="Account" asp-action="Checkout">Ganga frá pöntun</a></button>
        </div>
    </div>
</div>
