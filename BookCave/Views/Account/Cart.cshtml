@model List<BookCave.Models.ViewModels.CartViewModel>

<h1>Karfa</h1>
<div class="container">
    <div class="row"></div>
        <div class="col-lg-12 cl-md-6 col-sm-6 col-xs-6">
            <table class="table table-striped table-hover">
                <tr>
                    <th>Mynd</th>
                    <th>Titill</th>
                    <th>Magn</th>
                    <th>Afsláttur</th>
                    <th>Verð</th>
                    <th></th>
                </tr>
                @foreach(var item in @Model)
                {
                    <tr>
                        <td><img src="@item.Image"  height="200" width="120" /></td>
                        <td>@item.Title</td>
                        <td><input type="text" asp-for="@item.Quantity"><a asp-controller="Account" asp-action="ChangeQuantity" asp-route-quantity="@item.Id"><button class="btn btn-default">Uppfæra</button></a></td>
                        <td>@item.Discount %</td>
                        <td>@item.Price $</td>
                        <td><a asp-controller="Account" asp-action="RemoveCart" asp-route-id="@item.Id"><button class="btn btn-default">Fjarlægja út körfu</button></a></td>
                    </tr>
                }
            </table>
            <a asp-controller="Account" asp-action="ReviewOrder"><button class="btn btn-primary">Ganga frá pöntun</button></a>
        </div>
    </div>
</div>
